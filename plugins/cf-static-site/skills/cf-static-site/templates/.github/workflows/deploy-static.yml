name: Deploy site

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

concurrency:
  group: site-deploy
  cancel-in-progress: false

env:
  # Set to '.' to deploy from repo root instead of site/
  DEPLOY_DIR: site/

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-node@v6
        with:
          node-version: '22'

      - name: Check links
        run: node scripts/check-links.js

      - uses: cloudflare/wrangler-action@v4
        with:
          apiToken: ${{ secrets.CF_PAGES_API_TOKEN }}
          accountId: ${{ secrets.CF_PAGES_ACCOUNT_ID }}
          command: pages deploy ${{ env.DEPLOY_DIR }} --project-name <PROJECT_NAME> --branch <BRANCH>
