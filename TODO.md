# TODO — biohack-claude

Backlog for the plugin marketplace: the 16 plugins under `plugins/`, their skills,
commands and hooks, and `marketplace.json` itself.

Note that `plugins/tiered-todo/` is **generated** — everything in it except `README.md`
is assembled from the live files by `~/python-tui-lib/scripts/bundle-tiered-todo.sh`
(`task bundle-todo`). Work on it belongs in `~/python-tui-lib` or `~/.claude/`, not here;
items here should be about publishing, packaging, or the marketplace entry.

**Status markers:** `[ ]` open · `[wip]` in progress · `[verify]` implemented, verification
not yet run+recorded · `[x]` done (`## Done` only). The bracket also carries a delegation
tier, tier last — `[T4]`, `[wip T2]`, `[verify T3]`. Unranked items dispatch at the `T3`
default. See `~/CLAUDE.md` — Delegation.

## Open

_Nothing open._

## Watch

_Nothing being watched._

## Parked

_Nothing parked._

## Done

_Nothing recorded yet._
